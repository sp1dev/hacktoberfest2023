let q = 16;

const heading = document.querySelector('h1')
const color = [ 
 // 0    1      2        3      4  "red", "blue" "green", "yellow","orange"
];

function changeColor() {
  
  i++;
  if (i>4) {
    i=0;
  }
 heading.style.color = color;
}
